import React from 'react'
import { Button } from '@nextui-org/react'

const HomePage: React.FC = () => {
  return (
    <div>
      MacOS in the Web!!
      <br />
      Github Actions 自动部署实验
      <Button css={{ color: '$txt' }}>Click me</Button>
    </div>
  )
}

export default HomePage
